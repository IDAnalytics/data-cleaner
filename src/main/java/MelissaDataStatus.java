/**
 * MelissaDataStatus is used to indicate a success or failure
 * of a MelissaData call for the purposes of error handling
 * before post preProcess takes place.
 */
public enum MelissaDataStatus {
    SUCCESS,
    ERROR
}
